
func init() {
    {{range $_, $item := .Functions}}
    {{ if $item.Subscription }}registry.RegisterSubscription("{{ $item.CallName }}", {{ $item.Name }}, getEventName{{ $item.Name }} ){{ else }}registry.RegisterFunction("{{ $item.CallName }}", callAdapterFor{{ $item.Name }}){{ end }}{{end}}
}
